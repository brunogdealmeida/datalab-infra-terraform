# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# DBT credentials — never commit
dbt/profiles/profiles.yml
dbt/target/
dbt/dbt_packages/
dbt/logs/

# Local secret files loaded by file() in tfvars — never commit
secrets/

# Environment secrets
.env
*.pem
*.key
