name: Terraform

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy (dev, staging, prod)"
        required: true
        default: dev
        type: choice
        options: [dev, staging, prod]
      action:
        description: "Terraform action"
        required: true
        default: plan
        type: choice
        options: [plan, apply, destroy]

permissions:
  id-token: write
  contents: read
  pull-requests: write

concurrency:
  group: terraform-${{ github.event.inputs.environment || 'dev' }}
  cancel-in-progress: false

jobs:
  terraform:
    name: Terraform ${{ github.event.inputs.action || (github.ref == 'refs/heads/main' && 'apply' || 'plan') }}
    runs-on: ubuntu-latest

    env:
      ENV: ${{ github.event.inputs.environment || 'dev' }}
      ACTION: ${{ github.event.inputs.action || (github.ref == 'refs/heads/main' && 'apply' || 'plan') }}
      REGION: us-central1
      REPOSITORY_ID: datalab-images
      DBT_IMAGE_NAME: dbt
      GCP_PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to GCP
        id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.TERRAFORM_SA_EMAIL }}
          token_format: access_token

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Authenticate Docker to Artifact Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGION }}-docker.pkg.dev
          username: oauth2accesstoken
          password: ${{ steps.auth.outputs.access_token }}

      - name: Build and push dbt image
        run: |
          docker build \
            -t ${{ env.REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.REPOSITORY_ID }}/${{ env.DBT_IMAGE_NAME }}:${{ github.sha }} \
            -t ${{ env.REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.REPOSITORY_ID }}/${{ env.DBT_IMAGE_NAME }}:latest \
            ./dbt

          docker push ${{ env.REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.REPOSITORY_ID }}/${{ env.DBT_IMAGE_NAME }}:${{ github.sha }}
          docker push ${{ env.REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.REPOSITORY_ID }}/${{ env.DBT_IMAGE_NAME }}:latest

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~> 1.9"

      - name: Terraform Init
        run: make ENV=${{ env.ENV }} init

      - name: Terraform Plan
        id: plan
        run: make ENV=${{ env.ENV }} plan IMAGE_TAG=${{ github.sha }}
        continue-on-error: ${{ env.ACTION != 'plan' }}

      # STEP 1: Create infra (including Secret Manager)
      - name: Terraform Apply (infra)
        if: env.ACTION == 'apply'
        run: make ENV=${{ env.ENV }} apply IMAGE_TAG=${{ github.sha }}

      # STEP 2: Create secret version
      - name: Add dbt profiles secret version
        if: env.ACTION == 'apply'
        run: |
          if [ -z "${{ secrets.DBT_PROFILES_YML }}" ]; then
            echo "DBT_PROFILES_YML is empty or not available"
            exit 1
          fi

          cat > /tmp/profiles.yml <<'EOF'
          ${{ secrets.DBT_PROFILES_YML }}
          EOF

          gcloud secrets versions add dbt-profiles-yml \
            --data-file=/tmp/profiles.yml \
            --project=${{ env.GCP_PROJECT_ID }}

          rm -f /tmp/profiles.yml

      # STEP 3: Update Cloud Run with latest secret version
      - name: Terraform Apply (cloud run update)
        if: env.ACTION == 'apply'
        run: make ENV=${{ env.ENV }} apply IMAGE_TAG=${{ github.sha }}

      - name: Terraform Destroy
        if: env.ACTION == 'destroy'
        run: make ENV=${{ env.ENV }} destroy